-- Your original line (keep this)
loadstring(game:HttpGet('https://pastefy.app/IhOGH5JV'))()

-- Wait a tiny bit in case the original script sets up globals
wait(1.5)

-- Load Linoria (one of the best looking libraries in 2026 community)
local Library = loadstring(game:HttpGet('https://raw.githubusercontent.com/violin-suzutsuki/LinoriaLib/main/Library.lua'))()
local Window = Library:CreateWindow({
    Title = 'LEMON HUB DUELS',
    Center = true, 
    AutoShow = true,
    ShowCustomCursor = true,
    Resizable = true
})

local Tabs = {
    Main = Window:AddTab('Main'),
    Visuals = Window:AddTab('Visuals'),
    Misc = Window:AddTab('Misc')
}

-- Main Tab (Combat / Steal focused - matches your screenshot)
local MainSection = Tabs.Main:AddLeftGroupbox('Combat')

MainSection:AddToggle('BatAimbot', {
    Text = 'Bat Aimbot',
    Default = false
}):OnChanged(function(v)
    -- If the original script exposes something like _G.BatAimbot = v then do it here
    -- Otherwise many people just set a global the script might check
    _G.BatAimbotEnabled = v   -- common pattern
end)

MainSection:AddSlider('AimbotRadius', {
    Text = 'Aimbot Radius',
    Default = 50,
    Min = 10,
    Max = 150,
    Rounding = 1,
    Suffix = ''
}):OnChanged(function(v)
    _G.AimbotRadius = v
end)

MainSection:AddToggle('SpamBat', { Text = 'Spam Bat', Default = false })
MainSection:AddToggle('AutoAttack', { Text = 'Auto Attack', Default = false })
MainSection:AddToggle('AutoSteal', { Text = 'Auto Steal', Default = false })
MainSection:AddSlider('StealRange', {
    Text = 'Steal Range',
    Default = 66.2,
    Min = 20,
    Max = 120,
    Rounding = 1
})

-- Right side example
local RightSection = Tabs.Main:AddRightGroupbox('Movement / Boost')

RightSection:AddToggle('SpeedBoost', {
    Text = 'Speed Boost',
    Default = false
})
RightSection:AddSlider('BoostSpeed', {
    Text = 'Boost Speed',
    Default = 60,
    Min = 20,
    Max = 200,
    Rounding = 0
})

RightSection:AddToggle('InfJump', { Text = 'Inf Jump', Default = false })
RightSection:AddSlider('InfJumpPower', {
    Text = 'InfJump Power',
    Default = 80,
    Min = 50,
    Max = 200
})

RightSection:AddToggle('SpinBot', { Text = 'Spin Bot', Default = false })
RightSection:AddSlider('SpinSpeed', {
    Text = 'Spin Speed',
    Default = 50,
    Min = 10,
    Max = 150
})

-- Add more to match your screenshot (Auto Jump on Steal, Steal Pause, Galaxy Bright, etc.)
-- Example:
MainSection:AddToggle('AutoStealDuels', { Text = 'Auto Steal Duels', Default = false })
RightSection:AddToggle('OptimizerXRay', { Text = 'Optimizer + XRay', Default = false })
RightSection:AddToggle('GalaxyBright', { Text = 'Galaxy Bright', Default = false })

-- Bottom button
Tabs.Main:AddButton({
    Text = 'SAVE CONFIG',
    Func = function()
        Library:Notify('Config saved! (mock - implement saving if needed)', 3)
        -- You can add file save withwritefile if executor supports
    end
})

-- Optional: Discord label
Window:ShowCustomCursor()
Library:SetWatermark('discord.gg/lemonhub')  -- or whatever the real one is

print("Lemon-style UI loaded on top!")
